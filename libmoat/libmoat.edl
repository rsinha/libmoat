enclave {
  untrusted {
    void _shal_printDebugOnHost([string,in] const char *str);
    void _shal_outputToHost([in, size=len] void *buf, size_t len);

    /* automatically copies unprotected memory contents into buf */
    void _shal_inputFromHost([out, size=len_max] void *buf, size_t len_max, [out] size_t *len_actual);
  };
};
