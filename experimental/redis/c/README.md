# Using hiredis: Redis client API in C

## Installation
Linux: `apt-get install libhiredis0.13`  
Mac OS: `brew install hiredis`  

# Run `hello` program
`hello.c` is an example program that demonstrates how to run PING, GET, and SET using hiredis.
```
gcc hello.c -o hello
./hello
```
