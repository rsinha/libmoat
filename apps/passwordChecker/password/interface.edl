enclave {
  include "common.h"
  /* list enclave calls here */
  trusted {
    public uint64_t enclave_test();
    public uint32_t pw_setup([in, size=pwlen] const uint8_t *password, uint32_t pwlen, [out, size=bloblen_in] uint8_t *blob, uint32_t bloblen_in);
    public uint32_t pw_check([in, size=pwlen] const uint8_t *password,
	uint32_t pwlen, [in, size=bloblen] uint8_t *blob,
	uint32_t bloblen);
  };
  from "../../../../libmoat/api/libmoat.edl" import *;

};
